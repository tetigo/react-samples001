import React from "react";

function Icon() {
  const styles = {
    width: "5px",
    height: "5px",
    backgroundColor: "blue"
  };
  return <div style={styles} />;
}
export default Icon;
